{
{%- for key, value in terraform_result.outputs.items() -%}
  {%- if value.value is string -%}
    {{ "\"" + key + "\"" + ":\"" + value.value + "\"," }}
  {%- else -%}
    {{ "\"" + key + "\"" + ":" }}{{ value.value }}{{"," if not loop.last else ""}}
  {%- endif -%}
{%- endfor -%}
}